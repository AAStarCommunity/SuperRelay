{
  "name": "super-relay-web-ui",
  "version": "0.1.5",
  "description": "SuperRelay Web UI - Swagger UI and Dashboard",
  "private": true,
  "scripts": {
    "dev": "npm run serve",
    "serve": "npx http-server swagger-ui -p 9000 -c-1",
    "build": "echo 'Building Swagger UI assets...'",
    "clean": "rm -rf swagger-ui/dist"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/alchemyplatform/rundler"
  },
  "license": "LGPL-3.0-only"
}